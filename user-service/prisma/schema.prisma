datasource db {
    url      = env("DATABASE_URL")
    provider = "postgresql"
}

generator client {
    provider = "prisma-client-js"
}

model User {
    id       String   @id @default(uuid())
    nick     String   @unique
    email    String   @unique
    password String
    joined   DateTime @default(now())
}
